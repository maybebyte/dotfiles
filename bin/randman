#!/usr/bin/env sh
# lists man pages from specified sections, cuts out the names, shuffles them,
# and grabs the first page (like drawing the top card from a shuffled deck).
# Then passes that result to man for viewing.
man "$(find /usr/share/man/man{1,5,7,8} | sort -R | head -1)"
